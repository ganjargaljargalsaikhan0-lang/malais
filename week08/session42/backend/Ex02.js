


const item = "Coffee";
const qty = 3;
const unitPrice = 4.5;

console.log(`Item:  ${item}` );
console.log(`Quantity:  ${qty}` );
console.log(`Total: $ ${(qty * unitPrice)}` );
console.log(`You ordered  ${qty}  ${" x " } ${item } ${" for $"} ${(qty * unitPrice)}  ${" total."} `);